var hardCodedCharacters = (function() {

  var demo = [
    nif.data,
    vos.data
  ];

  var all = [
    nif.data,
    vos.data,
    orrin.data,
    nefi.data,
    ro.data, // kia
    marika.data // retired
  ];

  // exposed methods
  return {
    demo: demo,
    all: all
  };

})();
