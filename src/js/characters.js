var hardCodedCharacters = (function() {

  var load = [
    nif.data,
    vos.data,
    orrin.data
    // ro.data // kia
    // marika.data // retired
  ];

  // exposed methods
  return {
    load: load
  };

})();
